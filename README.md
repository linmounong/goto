### usage

```
$ go build
$ ./goto -h
Usage of ./goto:
  -db string
        filename of the SQLite file (default "goto.db")
  -dump
        dump http request
  -p int
        HTTP port (default 8096)
  -s    use local static assets
```

### docker

```
docker-compoes up -d
```
